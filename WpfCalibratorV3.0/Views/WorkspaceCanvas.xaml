<UserControl x:Class="WpfCalibrator.Views.WorkspaceCanvas"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:viewmodels="clr-namespace:WpfCalibrator.ViewModels.WidgetViewModel;assembly=WpfCalibratorV3.0"
                    xmlns:views="clr-namespace:WpfCalibrator.Views;assembly=WpfCalibratorV3.0"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="WidgetTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <ItemsControl ItemsSource="{Binding ActiveWidgets}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="#1E1E1E" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding LeftPosition, Mode=TwoWay}"/>
                    <Setter Property="Canvas.Top" Value="{Binding TopPosition, Mode=TwoWay}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type viewmodels:Matrix3DWidgetViewModel}">
                    <views:Matrix3DSurfaceWidget />
                </DataTemplate>
                <DataTemplate DataType="{x:Type viewmodels:LegacyWidgetViewModel}">
                    <ContentControl Content="{Binding}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ControlView}" Value="textbox">
                                        <Setter Property="ContentTemplate" Value="{StaticResource ScalarTextBoxTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="matrixtable">
                                        <Setter Property="ContentTemplate" Value="{StaticResource MatrixTableTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="digital">
                                        <Setter Property="ContentTemplate" Value="{StaticResource WidgetDigitalTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="sliderhorizontal">
                                        <Setter Property="ContentTemplate" Value="{StaticResource SliderHorizontalTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="slidervertical">
                                        <Setter Property="ContentTemplate" Value="{StaticResource SliderVerticalTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="gaugecircular270">
                                        <Setter Property="ContentTemplate" Value="{StaticResource GaugeCircular270Template}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="gaugearc120">
                                        <Setter Property="ContentTemplate" Value="{StaticResource GaugeArc120Template}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="timeplot">
                                        <Setter Property="ContentTemplate" Value="{StaticResource TimePlotTemplate}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ControlView}" Value="radartracker">
                                        <Setter Property="ContentTemplate" Value="{StaticResource RadarTrackerTemplate}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </DataTemplate>
            </ItemsControl.Resources>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type viewmodels:BaseWidgetViewModel}">
                    <Border x:Name="WidgetBorder" BorderBrush="#444444" BorderThickness="1" Background="#2D2D2D" Width="{Binding Width, Mode=TwoWay}" Height="{Binding Height, Mode=TwoWay}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="24"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Background="#333333" MouseDown="WidgetHeader_MouseDown">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="5,0">
                                    <TextBlock Text="{Binding Title}" Foreground="White" VerticalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="⚙" Width="20" Height="20" Margin="0,0,5,0" Background="Transparent" Foreground="Gray" BorderThickness="0" Click="WidgetSettings_Click">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ControlView}" Value="Matrix3DSurface">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                    <Button Content="X" Width="20" Height="20" Background="Transparent" Foreground="Gray" BorderThickness="0" Click="WidgetClose_Click"/>
                                </StackPanel>
                            </Grid>
                            <ContentControl Grid.Row="1" Content="{Binding}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>