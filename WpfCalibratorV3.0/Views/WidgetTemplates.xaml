<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:viewmodels="clr-namespace:WpfCalibrator.ViewModels"
                    xmlns:models="clr-namespace:WpfCalibrator.Models">

    <!-- Базовый стиль заголовков панелей -->
    <Style x:Key="LocalBlockHeaderStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFFF00"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontFamily" Value="Consolas"/>
    </Style>

    <!-- 1. ШАБЛОН: Цифровой дисплей телеметрии -->
    <DataTemplate x:Key="WidgetDigitalTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Config.Name}" Foreground="#00E5FF" FontSize="14" FontWeight="Bold" FontFamily="Consolas"/>
                <TextBlock Text="{Binding Config.Comment}" Foreground="#666666" FontSize="11" TextWrapping="Wrap" FontFamily="Consolas"/>
            </StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding CurrentValue}" Foreground="#00E5FF" FontSize="26" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>
    </DataTemplate>

    <!-- 2. ШАБЛОН: Скалярный параметр (ввод значения) -->
    <DataTemplate x:Key="ScalarTextBoxTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Config.Name}" Foreground="#00FF00" FontSize="15" FontWeight="Bold" FontFamily="Consolas"/>
                <TextBlock Text="{Binding Config.Comment}" Foreground="#888888" FontSize="11" TextWrapping="Wrap" FontFamily="Consolas"/>
            </StackPanel>
            <TextBox Grid.Column="1" Text="{Binding CurrentValue, UpdateSourceTrigger=PropertyChanged}" 
                     Background="#222222" Foreground="White" BorderBrush="#555555" 
                     Padding="6" VerticalAlignment="Center" HorizontalContentAlignment="Center" FontSize="16" FontWeight="Bold" FontFamily="Consolas"/>
        </Grid>
    </DataTemplate>

    <!-- 3. ШАБЛОН: Многомерная таблица LUT -->
    <DataTemplate x:Key="MatrixTableTemplate">
        <StackPanel>
            <Grid Margin="0,0,0,5">
                <TextBlock Text="{Binding Config.Name}" Foreground="#FF9100" FontSize="16" FontWeight="Bold" FontFamily="Consolas"/>
                <TextBlock Foreground="#FF9100" FontSize="11" FontFamily="Consolas" HorizontalAlignment="Right">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="Размерность: {0} x {1}">
                            <Binding Path="Config.Rows"/>
                            <Binding Path="Config.Cols"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
            <TextBlock Text="{Binding Config.Comment}" Foreground="#A0A0A0" FontSize="12" Margin="0,0,0,8" FontFamily="Consolas"/>

            <ItemsControl ItemsSource="{Binding MatrixData}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Rows="{Binding Config.Rows}" Columns="{Binding Config.Cols}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewmodels:MatrixCellViewModel}">
                        <Border BorderBrush="#333333" BorderThickness="1" Background="#222222" Margin="1">
                            <TextBox Text="{Binding ValueText, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="Transparent" Foreground="White" BorderThickness="0" 
                                     HorizontalContentAlignment="Center" VerticalContentAlignment="Center" 
                                     Padding="6" FontSize="14" FontFamily="Consolas"/>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>
